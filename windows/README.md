# Windows 平台文件

Windows 目前尚未完成 BLE、算法 SDK 和后台服务的构建验证，因此本目录暂不提供启动器或部署脚本。

跨平台网页资源位于 `web/`，可直接由 Windows 上的任意静态服务器托管。若后续完成 Windows 网关验证，请将 PowerShell 启动、Windows Service 配置、依赖锁定和验收说明放在此目录，不要放入 `web/` 或 `neurobridge/`。
