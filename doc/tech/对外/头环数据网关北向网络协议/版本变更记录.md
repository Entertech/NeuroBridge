# 版本变更记录

本文件由 `neurobridge/version_registry.toml` 生成；版本台账是唯一来源。已锁定区间不可回写，未锁定变更不会修改已发布对外文档。

## 已锁定的发布区间

### v0.1 → v0.2

- 状态：`locked`；锁定时间：2026-08-05。
- 锁定依据：已发布的 B 端 头环数据网关北向网络协议 v0.2 PDF。
- 约束：此区间的 Markdown/PDF 及其语义不再回写；后续变更只能创建新的未锁定记录。

## 当前内部预发布版本

- 版本：v0.3；状态：`prerelease`。
- 基线：v0.2；仅保留此一份内部预发布 Markdown。
- 升格规则：收到明确发布指令后，将该文档转换为独立的对外版本 Markdown，更新台账和锁定记录；CI 成功生成该版本 PDF Artifact 后方可合入发布。

## 未锁定变更

### 2026-08-10：2026-08-10-ssh-listen-address-startup-order

- 变更：SSH 运维配置器为地址受限的 ssh.service 安装 network-online 依赖和监听地址就绪等待，消除系统启动时静态 IP 晚于 sshd 导致服务失败的竞态；仍仅监听已确认的固定私有地址。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；现场重启日志显示 sshd 在 192.168.31.33 尚未配置时启动并报 Cannot assign requested address；修复仅涉及网关内部 SSH 运维，不改变 B 端协议或已发布对外文档。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-05：2026-08-05-capture-package-pdf-export

- 变更：一键保存 ZIP 在会话根目录附带已发布的头环数据采集包格式说明 v0.1 PDF，并在 manifest 的 documentation 字段记录其路径、版本和 SHA-256。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求调整导出物；已发布的采集包格式说明 v0.1 保持锁定，未改写其 Markdown。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-05：2026-08-05-replay-get-latest-cursor

- 变更：录播中 getLatest 优先返回当前 WebSocket 连接已回放的有效算法结果；尚未订阅时返回录制中的最新有效算法结果。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；未收到更新对外文档的明确指令；运行时保持已发布 v0.2 的错误码兼容性。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-05：2026-08-05-flowtime-document-profile-poc

- 变更：macOS 采集 POC 按已核对的 Flowtime 文档订阅 FF31、FF32、FF51 并使用 FF21 启停；移除不存在的 FF52 订阅。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；已发布 v0.2 保持锁定；未收到更新对外文档的明确指令，且对外协议不应暴露设备 BLE 特征或固定包长。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-05：2026-08-05-macos-native-affective-bridge-poc

- 变更：macOS POC 接入锁定的 AffectiveCloud C++ SDK，构建本地算法 bridge 并输出既有算法字段结构；已完成源码构建和合成输入烟雾测试，未完成真实数据或 Ubuntu 验证。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；对外 v0.2 保持锁定，且未收到更新对外文档的明确指令；本变更不增加或修改 B 端可观察字段。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-06：2026-08-06-confirmed-device-profile-restoration

- 变更：记录一次错误的设备 profile 假设：曾错误加入未在设备规范定义的 FF52 并采用错误包长；后续由 2026-08-07 纠偏记录恢复为飞书设备规范。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；该内部记录保留变更轨迹；已发布对外 Markdown 未被改写，错误实现不构成已验证能力。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-06：2026-08-06-ubuntu-download-and-persistent-operations

- 变更：Ubuntu x86_64 网关新增 systemd 即装即启、文件化持久运行日志，以及专用有线网络上的已结束录播 ZIP 与日志快照下载 HTTP 服务。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求增加部署与运维能力，但未明确授权更新已锁定的 B 端对外协议；下载地址、端口和启用状态均由部署配置控制。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-06：2026-08-06-static-dhcp-address-allocation

- 变更：新增可切换的静态地址与 DHCP 地址分配模式；DHCP 以独立 dnsmasq/systemd 单元为 B 端分配地址并发布固定网关地址，支持独立禁用或移除。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求支持无屏网关的两种地址分配方案及低影响移除；已发布对外协议保持锁定，且端口仍由部署配置固定。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-06：2026-08-06-gateway-level-replay-controller

- 变更：录播改为网关级单一流程：头环未连接时首个 getLatest 或 subscribe 启动回放，后续订阅加入当前进度，重连时停止回放，结束后允许下一次数据指令重新开始。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求按设备连接状态自动启动且避免重复录播流程；未授权改写已锁定的对外协议 Markdown。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-06：2026-08-06-replay-subscriber-failure-isolation

- 变更：网关级录播为每个订阅者使用独立有限投递队列；单个发送失败或积压订阅会被移除，不再中止其他订阅者的录播。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；PR 审阅发现共享录播会被单个客户端发送异常中止；修复仅增强可靠性，未授权修改已锁定对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-06：2026-08-06-replay-connection-lifecycle-and-loop

- 变更：最后一个 B 端 WebSocket 连接断开时停止录播并重置进度；仍有连接时，录播文件到末尾后从首条循环，循环边界继续发送 replayEnded。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求明确演示录播的连接生命周期和循环行为；已更新内部预发布说明，未获授权改写已锁定的对外 v0.2 Markdown。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-flowtime-spec-profile-correction

- 变更：按 Flowtime headband 蓝牙通信协议（2023-06-19）纠正设备 profile：仅订阅 FF31、FF32、FF51；移除未定义的 FF52；FF31=20 字节、FF51=1 字节。FF51 原始字节持久化并作为 hr.raw 输出；Ubuntu 受控 bridge POC 也只读取这两类原始数据，算法默认保持关闭。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求依据飞书设备通信规范纠正实现；已发布对外 v0.2 与采集包格式说明 v0.1 保持锁定，未改写对外 Markdown。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu-automatic-algorithm-bridge-install

- 变更：Ubuntu 安装器自动以锁定 SDK 构建并安装算法 bridge；网关在未配置 algorithm.command 时使用固定服务路径，部署人员只需控制 algorithm.enabled。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求 Ubuntu 算法开箱可用且无需手工填写 bridge 路径；不改变 B 端可观察字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-algorithm-enabled-by-default

- 变更：算法配置和 Ubuntu 部署模板默认启用算法；无需手动设置 algorithm.enabled 或 algorithm.command。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求算法默认启用；不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu24-offline-algorithm-deployment

- 变更：Ubuntu 24.04 网关安装改为只使用受控构建机生成的离线包；目标机不访问或登录 GitHub、APT、PyPI，SDK 源码和依赖均在本地校验后使用。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求外部机器完全离线配置且无需 GitHub 账号；不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu24-one-command-offline-installer

- 变更：新增 Ubuntu 24.04 离线一键安装入口，默认发现源码目录同级的离线包并自动申请管理员权限。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求缩短目标机部署指令；不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-source-only-ubuntu-deployment

- 变更：Ubuntu 24.04 部署改为随 NeuroBridge 源码提交锁定的算法 SDK 与 NumCpp 源码；目标机可匿名 Git 获取源码，源码到位后的安装、bridge 构建、Python 本地更新和运行均不访问互联网。离线包流程已撤销。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户澄清无需 U 盘或离线包；只要求获取完整源码后不再联网，且外部机器无需登录 GitHub。不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu24-online-environment-preparation

- 变更：新增 Ubuntu 24.04 一次性联网环境准备脚本，用于安装系统依赖和锁定 Python 运行依赖；完成后可断网，后续部署仍只使用本地源码。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求提供可联网完善环境、随后断网运行的脚本；不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-cross-platform-bridge-build-diagnostics

- 变更：统一 macOS 与 Ubuntu 的 NumCpp 无 Boost 构建参数；Ubuntu 每次从干净的 CMake 构建目录生成 bridge 并保存构建日志，新增不含现场配置和原始数据的诊断日志收集脚本。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户反馈 Ubuntu bridge 存在多项编译问题，要求跨设备复核并提供日志；不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu24-sdk-header-include-fix

- 变更：补全随源码交付的 AffectiveCloud SDK 的 DSPBCG.h 对 <limits> 的直接依赖，修复 Ubuntu 24.04 GCC 13 构建时 std::numeric_limits 未声明而导致 bridge 编译中止的问题。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供 Ubuntu 24.04 构建诊断日志后确认该 SDK 头文件依赖被 macOS 间接包含掩盖；修复不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu24-sdk-ffttool-cstring-fix

- 变更：补全随源码交付的 AffectiveCloud SDK 的 FFTTool.cpp 对 <cstring> 的直接依赖，并使用 std::memset，修复 Ubuntu 24.04 GCC 13 构建时 memset 未声明而导致 bridge 编译中止的问题。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供新一轮 Ubuntu 24.04 构建诊断日志后确认上一处 DSPBCG.h 修复已经生效；修复不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu24-sdk-wavelettool-standard-library-fix

- 变更：补全随源码交付的 AffectiveCloud SDK 的 WaveletTool.cpp 对 <cctype>、<cstring> 和 <stdexcept> 的直接依赖，并统一使用 std::strcmp，修复 Ubuntu 24.04 GCC 13 构建时 strcmp 和 invalid_argument 未声明而导致 bridge 编译中止的问题。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供新一轮 Ubuntu 24.04 构建诊断日志后确认 FFTTool.cpp 修复已经生效；修复不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu24-sdk-public-api-cstdint-fix

- 变更：补全随源码交付的 AffectiveCloud SDK 公开 Affective.h 对 <cstdint> 和 <vector> 的直接依赖，并使用 std::uint8_t，修复 Ubuntu 24.04 GCC 13 编译 bridge 时 uint8_t 未声明及后续算法调用签名错误的问题。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供新一轮 Ubuntu 24.04 构建诊断日志后确认 SDK 本体已经完成编译，错误发生在 bridge 对公开头文件的首次包含；修复不改变 B 端字段、请求或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-automatic-replay-recording-selection

- 变更：离线录播在显式 replay_recording_id 为空、无效或对应空会话时，自动遍历 recording.directory，选择最新的非空历史录制会话；有效的显式会话仍优先。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求网关在录播模式自动发现特定目录下的历史数据；不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ubuntu-ssh-operations-entrypoint

- 变更：Ubuntu 环境准备新增 OpenSSH Server 但默认停用；新增交互式一键 SSH 运维配置与严格配置脚本，要求受控运维公钥、已配置的私有监听地址与允许来源网段，禁用 root/密码登录和转发；登录后可用 neurobridge-ops 查询服务状态、历史日志、实时日志并启停服务，专用运维账户无任意 root 权限。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求完成网关 SSH 运维能力；该入口不改变 B 端请求、数据字段或已发布的对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ssh-replay-operations-guide

- 变更：新增内部《网关 SSH 运维与 B 端录播联调指南》，说明仅网关与 B 端专用有线直连时的环境准备、录播配置、首次 SSH 引导、实时状态与日志查询、服务启停、联调验证、排障和撤销步骤。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求将 SSH 运维与离线录播联调操作说明落库；不修改已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ssh-staged-update-and-reload

- 变更：SSH 运维新增 neurobridge-ops update：仅应用管理员预置在 /srv/neurobridge-release 的完整审核版本并调用 reload-ubuntu.sh 重新加载网关；拒绝网络拉取、SSH 传入路径、符号链接、非 root 所有或可被组/其他用户写入的暂存版本。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求 SSH 支持一键更新代码和重新加载；实现保持专用网络边界与运维账户最小权限，不改变已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-07：2026-08-07-ssh-password-authentication

- 变更：SSH 运维认证从公钥改为受限运维账户的密码登录；一键配置器隐藏输入并二次确认至少 12 位密码，禁用 root 和公钥登录，保留私网来源限制与最小 sudo 权限。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求 SSH 使用账号密码登录；不改变已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-operation-audit-log

- 变更：SSH 运维新增 root-owned 命令网关和系统审计日志：每个受限运维命令记录 sudo 确认的操作者、动作、参数摘要及开始/成功/失败结果，并可通过 neurobridge-ops audit 查询；不记录密码、SSH 会话内容或业务原始数据。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求新增 SSH 操作日志；该变更仅涉及网关内部运维，不改变已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-kylin-b-side-operations-guide

- 变更：内部 SSH 运维与录播联调指南补充麒麟 B 端候选环境的版本和架构确认、OpenSSH 与浏览器前置检查、专用有线静态 IP、隔离前软件准备、本地联调页托管、端口策略和现场验收要求；网关部署目标仍为 Ubuntu 24.04 x86_64。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户说明 B 端主机大概率采用麒麟国产系统，并明确要求补充内部联调指南；实际产品版本、CPU 架构和现场 POC 状态尚待确认，不改变已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-single-ssh-operator-update-flow

- 变更：SSH 更新流程简化为单一受信任运维账号：一键配置将 /srv/neurobridge-release 交给该账号，同一账号通过 rsync 同步完整版本并执行 neurobridge-ops update；更新仍校验固定目录、完整性、所有者、写权限和符号链接，并保留操作审计。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求暂不区分系统管理员与普通运维，优先采用简单的单账号操作流程；该账号提供的代码会以 root 安装，因此按网关管理员凭据管理，不改变已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-project-working-directory

- 变更：SSH 运维取消独立 /srv/neurobridge-release 暂存目录；一键配置将当前完整源码初始化到运维账号固定的 ~/NeuroBridge 项目工作目录，新增 neurobridge-ops project 查询实际路径，代码查看、git 更新、隔离同步和 neurobridge-ops update 均围绕同一目录执行。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求 SSH 登录后明确项目所在目录，并直接在项目目录完成后续操作，不再维护单独的版本暂存目录；不改变已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-six-digit-password

- 变更：SSH 运维账号密码规则调整为正好 6 位数字；交互式配置继续隐藏输入并二次确认，非交互配置执行相同格式校验，且不绕过操作系统自身密码策略。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求使用 6 位数字密码；该规则仅适用于专用网线直连、固定来源 IP 且不接入公网/无线网/普通局域网的封闭现场，不改变已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-listen-address-default

- 变更：SSH 一键配置的网关私有监听 IP 默认使用当前设备已配置的全局私有 IPv4；已有 SSH 会话时优先采用会话连接到的本地地址，否则采用检测到的第一个非回环、非链路本地地址，未检测到时不展示默认值。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求监听地址默认采用当前设备 IP，设备没有符合条件的 IP 时不显示默认；不改变已发布 B 端对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-socket-and-cidr-startup-fix

- 变更：SSH 一键配置兼容 Ubuntu 24.04 的 socket 激活：环境准备停用 ssh.socket，严格配置器在启用地址受限的 ssh.service 前关闭通配 socket 并在失败时恢复原 service/socket 状态；允许来源 IP/CIDR 在写入 sshd 前规范化，避免主机地址搭配网段掩码导致配置无效。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；现场首次配置暴露 ssh.socket 占用或绕过监听地址的风险，以及非规范 CIDR 被 sshd 拒绝的问题；修复仅涉及网关内部 SSH 运维，不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-leftover-listener-cleanup

- 变更：SSH 严格配置器在关闭 socket 激活后停止 ssh.service，并清理其控制组内因 Ubuntu KillMode=process 遗留的旧 sshd 监听进程；确认 SSH 端口释放后只启动一次地址受限服务，远程会话遇到残留时拒绝强制清理并要求改用本地控制台。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；现场日志确认旧 sshd 进程在 service 停止后继续占用 22 端口，导致新配置无法绑定网关地址；修复只影响网关内部 SSH 服务切换，不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-six-digit-password-policy-compatibility

- 变更：SSH 配置器为专用运维账户显式使用 shadow-utils chpasswd 的 SHA512 加盐哈希方法写入正好 6 位数字密码，避免 Ubuntu PAM 最短 8 位策略造成一键配置失败；不修改全局 PAM 策略，密码仍仅通过标准输入传递且不写入配置或日志。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；现场配置确认 Ubuntu PAM 拒绝此前已约定的 6 位运维密码；用户仍要求使用 6 位数字。本修复只影响封闭专用网络中的网关运维账户，不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-minimum-password-and-case-insensitive-confirmation

- 变更：SSH 运维密码规则由正好 6 位调整为至少 6 位纯数字，交互式与非交互式入口使用相同校验；一键配置的最终 YES 确认改为忽略大小写。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求密码不少于 6 位，并优化最终确认输入的大小写兼容性；该变更仅涉及网关内部运维交互，不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-field-troubleshooting-retrospective

- 变更：内部 SSH 运维指南沉淀首次现场联调问题与成功路径：补充临时局域网到最终网线直连的地址切换、/32 与网段 CIDR 语义、显式 neuroops 用户名、配置后验收清单、按 service/journal/端口/账户顺序排查，以及 PAM 最短密码、ssh.socket 和残留 sshd 冲突的现象、根因与恢复步骤。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户确认当前环境已能通过 SSH 登录，并要求将本轮对话中遇到的问题落库；内容只涉及网关内部运维流程，不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-quick-setup-mode

- 变更：保留 SSH 原完整交互配置流程，并为常规单网卡、单 B 端场景新增 setup-ssh-operations.sh --quick：自动使用 neuroops、端口 22 和检测到的当前网关私有 IP，只询问或接收一个 B 端 IP 并强制转换为单机 /32 来源，继续保留隐藏密码二次确认和最终确认。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户反馈现有 SSH 配置操作较复杂，要求保留原逻辑并提供一键配置；该变更仅简化网关内部运维入口，不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-quick-text-configuration

- 变更：SSH 快速模式改为读取 Git 忽略的 config/ssh-operations.txt，支持 operator_user、password、listen_address、allow_from 和 port；字段缺失或为空时才交互询问，允许在权限 600 的现场文件中保存至少 6 位数字明文密码，且不回显、不写日志。保留无参数完整模式和 --quick B端IP 覆盖方式。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求增加 TXT 格式的 SSH 配置文件，由 --quick 直接读取、缺参再询问，并明确允许密码以明文写入文件；该变更只涉及网关内部运维入口，不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-controlled-master-update

- 变更：SSH 运维的 neurobridge-ops update 改为唯一受控更新入口：以受信任运维账户检查固定 Git 工作树无本地修改，切换至 master、仅快进拉取批准上游版本，再校验工作树安全性并重载服务；不再支持运维人员通过 SSH 手工运行 Git、替换源码目录或传入临时路径。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求以 neurobridge-ops 替代 SSH 场景中的直接 Git 操作，并要求固定切换 master 后拉取；该变更只约束网关内部部署流程，不改变已锁定的 B 端北向协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-ssh-boot-autostart-documentation

- 变更：内部 SSH 运维文档明确启动边界：环境准备阶段 SSH 默认停用；一键配置成功后 ssh.service 独立于 neurobridge.service 开机自启，ssh.socket 保持停用；现场 TXT 不会随开机自动重读，参数或监听 IP 变化后须重新执行 --quick，并在重启后验收 enable/active/监听状态。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求将 SSH 是否随网关启动及配置生效方式补充进对应文档；内容只澄清现有网关内部运维行为，不改变 B 端请求、字段或已发布对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-default-offline-wired-network-configuration

- 变更：Ubuntu 安装器新增受控 Netplan 配置器：在无网线、DHCP、DNS 或局域网时，为唯一物理以太网口自动写入默认的封闭直连静态地址；多网口、无网口或已有明确 Netplan 配置时拒绝猜测或覆盖。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求在不接网络的前提下自动配置默认 IP；随后确认当前网关若已联网不得被自动改写，因此配置器检查 IPv4 地址和默认路由，默认拒绝替换活跃接口。该变更仅影响网关内部部署，且不改变已锁定 B 端协议的请求、字段或版本。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-network-config-install-order-and-interface-migration-safety

- 变更：修复首次安装时网络配置器先于 gateway.toml 创建而失败的问题；已管理 Netplan 文件改为默认拒绝迁移到另一物理网口，迁移须显式确认，旧接口仍有地址或路由时还须显式允许替换活跃接口。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；PR 审阅发现首次安装顺序错误和已管理网口迁移会静默删除旧接口配置的风险；修复仅涉及网关内部部署，不修改已锁定的 B 端协议或外部评审指南。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-10：2026-08-10-network-config-ipv6-and-rfc1918-safety

- 变更：网络配置器将活跃网口保护扩展到 IPv6 全局地址和默认路由，并仅允许 RFC1918 IPv4 专用链路地址，避免误断 IPv6 管理网络或接受回环、链路本地地址。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；PR 审阅发现 IPv4-only 检查会遗漏 IPv6 管理网络，且 Python 的 is_private 会接受回环和链路本地地址；修复仅涉及网关部署保护与参数校验，不修改已锁定的 B 端协议或外部评审指南。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-offline-runtime-diagnostics

- 变更：为无 Codex、无互联网的 N100/N150 银河麒麟 V10 现场新增一键运行诊断包：采集系统/内核/硬件、systemd 与当前/上次启动 journal、应用日志、USB/TTY/驱动、网络、资源、依赖及二进制校验，排除配置内容、环境变量、凭据和原始业务数据，执行文本脱敏并生成 SHA-256；同时补充 UTC 毫秒日志、启动配置摘要、BLE 阶段耗时、采集包/字节/异常统计、算法耗时和下载审计。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求审查 PR #13，并考虑 N100 现场没有 Codex 时的故障定位，补充尽可能详细且可安全导出的日志。该变更只增强网关内部可观测性和运维取证，不改变 B 端请求、响应、事件、字段或已发布北向协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-local-browser-access-strategy

- 变更：当前单机银河麒麟场景默认改为 local_browser：网关在 127.0.0.1 提供本机 HTTP 页面、WebSocket 和可选下载，使用固定 Origin/Host 校验且不再要求 B 端专用网线；通过策略模式保留 wired_b_side 兼容方案，两种策略复用相同消息契约、订阅、录播和业务处理。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户确认当前只使用一台网关主机及同机浏览器，并要求按策略模式保留旧有线方案与后续扩展。该变更调整部署拓扑和访问安全边界，不改变 WebSocket 请求、响应、事件字段或报文 protocolVersion；未收到更新已锁定对外文档的明确授权。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-serial-device-protocol-confirmations

- 变更：锁定 N100/N150 最终银河麒麟 x86_64 镜像及 SHA-256；串口发现改为稳定顺序逐个探测、首个固定握手即 ACK 并停止遍历；确认 0xE1/0xE0 为无响应单字节指令、序列号为大端 16 位递增回绕，并复用现有 EEG/HR 原始算法输入。麒麟诊断脚本补充系统构建信息采集。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户逐项确认设备侧串口协议、单设备部署前提、序列号和算法兼容规则，并确定最终安装镜像；这些内容只影响网关内部设备接入、部署与诊断，不改变 B 端请求、响应、事件、字段或已发布北向协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-serial-control-response-loss-telemetry-and-one-command-setup

- 变更：更正串口控制语义：0xE1/0xE0 在配置超时内收到任意非空响应即成功，响应原文不写日志；按 16 位大端扩展序列统计累计与区间期望包、唯一接收包、丢包数、丢包率、重复、乱序和迟到，并新增银河麒麟 x86_64 一键串口配置、权限校验、服务重启和现场日志指引。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确修正此前无响应结论，并要求采用业界口径计算丢包率、补充足以现场排障的日志，以及在无 Codex 的银河麒麟设备上提供简洁的一键配置。变更仅涉及网关内部设备接入、统计和运维，不改变 B 端请求、响应、事件、字段或已发布北向协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-multi-transport-technical-design-and-gap-closure

- 变更：新增多传输设备接入技术方案与需求追踪矩阵；设备策略改为注册表并只校验所选传输，BLE/串口统一输出含来源和适配器接收时间的 DevicePacket；串口候选增加 USB 父设备、物理路径和接口稳定排序，打开前配置 DTR/RTS，完整 28 字节设备帧写入不参与对外导出或录播的内部记录；原生 USB 在协议未确认时保留明确安全拒绝。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求为内部多传输 PRD 编写对应技术方案、检查未实现需求并实现当前可安全落地的部分；本次只调整网关内部设备接入、内部持久化、诊断和文档，不改变已锁定 B 端协议或已发布采集包格式。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-direct-device-setup-and-start-guide

- 变更：将银河麒麟 V10 内部手册重写为新设备直接上手流程，仅保留 USB 串口耳机的首次安装、一键配置、systemd 启停、本机页面、连接成功日志判断、常见故障和诊断包导出；删除背景论证、其他设备传输和其他访问拓扑说明。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求内部文档只说明新设备如何配置和启动程序，并排除蓝牙、网线等其他场景；该变更只简化内部部署操作说明，不改变已锁定 B 端协议或运行报文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-usb-serial-plug-and-handshake-diagnostics

- 变更：USB 串口发现新增 by-id/ttyACM/ttyUSB 节点清单与固定路径存在性记录；握手探测区分无任何字节和有字节但协议不匹配，只记录读取、丢弃、截断、最长固定前缀和耗时；数据流新增非法长度/包尾、重同步丢弃、有效帧超时和自动重枚举诊断，并在麒麟直操作手册中补充 USB 插入观测和日志判断表。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求插入 USB 后完整记录未发现设备、打开失败、无握手字节、错误握手、异常前导字节、非法数据帧、有效帧超时和重枚举，同时不得记录未知串口载荷或完整生理数据。该变更只增强网关内部可观测性和现场操作说明，不改变已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-usb-serial-interactive-diagnosis

- 变更：新增银河麒麟 V10 USB/串口插入一键识别脚本：交互提示先拔出记录基线、再在可配置超时窗口内插入；过程持续保存 lsusb/sysfs/TTY 前后快照、内核 journal、dmesg、udev 和网关 journal，结果明确区分成功生成 TTY、USB 已枚举但 TTY 超时、USB 未枚举超时，并始终保留日志目录、压缩包和 SHA-256。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户现场执行串口节点查询均为空，要求将 USB/TTY/驱动判断整合为有操作提示、明确超时结果和全程日志保存的一键脚本。该变更只影响网关内部麒麟运维与诊断，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-project-root-command-paths

- 变更：统一银河麒麟 V10 内部手册的脚本执行语义：操作员从当前 NeuroBridge 项目根目录直接执行 sudo ./linux/... 命令，不再预先进入 /opt/neurobridge；/opt/neurobridge 仅作为 systemd 实际安装目录说明。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户指出一键脚本就在当前项目的 linux 目录，不应要求切换到 /opt/neurobridge。该修正只涉及内部操作手册的命令路径，不改变应用部署位置、运行报文或已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-serial-python-environment-discovery

- 变更：麒麟串口配置脚本扩展 Python 环境发现：优先选择当前激活环境、项目 .venv/venv 或已安装 venv 中含 pyserial 的完整运行时，再回退到可完成配置的 Python 3.11+；新增 --python 绝对路径显式指定，并在无可用环境时输出可直接执行的 .venv 离线初始化命令。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户在当前项目根目录执行 setup-kylin-serial.sh 时报 NeuroBridge Python environment was not found，说明原脚本仅识别 /opt/neurobridge/venv 和 .venv 不足以覆盖实际现场。该变更仅增强内部配置工具和操作说明，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-project-local-runtime-artifacts

- 变更：银河麒麟项目目录直接运行时，默认将配置、备份、日志、录制和诊断产物统一写入项目 .runtime，Python 环境和离线依赖使用项目 .venv/venv 与 wheelhouse；相关目录加入 Git 忽略。新增项目内直接启动脚本，诊断工具的默认输出及临时工作目录也收口到 .runtime/diagnostics。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求项目运行产生的文件都保存在当前项目内，并将这些路径加入 .gitignore 避免误提交。该变更不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-one-command-python-setup

- 变更：新增银河麒麟项目 Python 环境一键初始化脚本：自动检查麒麟 x86_64 和 Python 3.11+，创建 .venv，wheelhouse 有离线包时使用离线安装，否则使用当前网络，最后执行依赖和导入校验。缓存、临时文件和过程日志仍全部保存在项目 .runtime。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户认为手工检查系统、准备 wheelhouse、创建虚拟环境和验证依赖的步骤过于复杂，要求简化。该变更仅影响麒麟内部部署工具和操作手册，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-project-local-python311-runtime

- 变更：针对已确定的银河麒麟 V10 SP1 2503 x86_64 镜像仅自带 Python 3.8.10 的情况，一键初始化脚本在系统无 Python 3.11+ 时自动使用或下载锁定 SHA-256 的 x86_64 Linux 独立 Python 3.11 运行时，解压到项目 python-runtime，不替换系统 Python。Linux 传递依赖锁定版本，wheelhouse 通过独立摘要清单逐文件校验；支持将运行时与完整 wheel 预先放入项目完成全离线初始化。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户在已确定麒麟镜像上执行一键脚本时报 Python 3.11+ is not installed；镜像 manifest 确认自带 Python 3.8.10。为避免要求现场人员手工改造系统 Python，将所需运行时收口到项目目录。该变更不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-serial-permission-and-usbutils-compatibility

- 变更：根据 N100/N150 麒麟实机诊断优化串口启动：打开设备前检查实际读写权限，账号已加入 TTY 所属组但当前会话未激活时通过 sg 自动以该组重启一次，失败则立即给出注销重登或重启提示；候选全部打开失败时保留 Permission denied 等根因，只有成功打开后未匹配固定帧才归类为握手失败；诊断脚本在旧版 lsusb 不支持 -nn 时自动回退。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；现场诊断确认 CP2102、cp210x 和 ttyUSB0 均正常，但登录会话未激活新加入的 dialout 组，程序将 Permission denied 次生汇总为握手失败；同时麒麟 usbutils 不支持 lsusb -nn。该变更只增强网关内部启动、错误分类和诊断兼容性，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-current-serial-detection-without-replug

- 变更：银河麒麟 USB 串口诊断默认改为直接遍历当前已连接设备，无需拔插；检测到 ttyACM、ttyUSB 或 by-id 仅作为候选，目标仍由网关固定握手确认。原拔插前后差分保留为显式 --plug-cycle 故障诊断，只用于区分物理 USB 未枚举、已枚举但未生成 TTY 和新 TTY 已生成。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户指出运行时本就会遍历多串口并通过握手确定目标，日常检测不应要求拔插 USB。该变更只调整网关内部麒麟诊断与操作流程，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-01：2026-09-01-kylin-interactive-onboarding-and-git-permission-recovery

- 变更：新增银河麒麟项目一键助手，以数字菜单串联首次 Python 初始化、当前 USB/串口检测、串口配置、启动和诊断导出；单项判断统一使用 yes/no，每步保存过程日志并提示下一条命令。助手还会在校验项目根目录后修复 root 遗留所有权和 Git 索引写权限，再以普通用户执行 fast-forward 更新；代码实际更新后退出并要求重新运行。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户在麒麟主机重启后遇到 Git 索引权限不足，并要求把下一步操作做成简单数字菜单和 yes/no 确认的一键流程。该变更仅涉及项目内部署助手、权限恢复和内部操作文档，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-kylin-single-file-operator-entry

- 变更：新增可单独传给旧设备的 neurobridge-kylin-bootstrap.sh：文件复制到项目根目录后可直接由 Bash 运行，验证项目边界并按 yes/no 修复 root 所有权和 Git 索引权限；在 PR #13 尚未合入 master 时识别当前分支、明确确认后切换并快进到 codex/serial-usb-transport，随后打开首次配置、启动和诊断菜单，过程保存到项目日志。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求把此前麒麟电脑上的多步执行说明收口为一个可直接运行的单文件。该变更只简化内部现场入口和权限恢复，不修改运行报文或已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-serial-control-response-classification-logging

- 变更：串口控制响应日志新增单字节 0x01、完整/包含/部分固定握手和其他非空响应的分类，并累计数据阶段握手数但不记录完整载荷；web/capture 改为可直接双击、纯浏览器本地解析运行日志的 USB 串口检查页，旧 macOS BLE POC 改由进程自动开始采集以解除对页面 API 的依赖；同时修复麒麟 systemd 245 无法解析 journalctl ISO 时间窗口的兼容性问题。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；现场日志显示 0xE1 后持续收到字节却无有效数据帧，需要在不采集生理原始数据的前提下区分真实 0x01 响应与被误认的周期握手；用户同时要求 capture 页面兼容本次方案且不依赖任何服务。该变更只增强内部串口可观测性和离线排障入口，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-gateway-hosted-capture-data-viewer

- 变更：web/capture 改为由当前网关 HTTP 服务托管的耳机实时数据查看页，只使用既有 WebSocket getStatus、subscribe 和 unsubscribe；订阅 eeg.raw、hr.raw、status 后支持 16/10 进制切换，逐字节空格分隔并以数据点分组显示 EEG 大端序列号、六路 24 位原始值、心率和页面推算丢包率。页面不再直接访问串口或离线日志，开始/停止仅控制当前网页订阅，不停止网关进程、设备连接或录制。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求两个网页只能依赖当前网关服务，capture 页面打印耳机数据并能控制数据开始/停止，同时不得干涉网关运行。该变更复用现有北向动作和字段，仅新增本机页面路由与显示逻辑，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-capture-diagnostic-log-export

- 变更：网关托管的 web/capture 页面新增一键导出本机诊断日志，记录连接与订阅状态、数据包/字节计数、最新序列号、丢包统计、解析异常和脱敏 WebSocket 协议记录；bytesBase64 与完整 EEG/心率原始字节不写入导出文件，并明确标记 rawPayloadExported=false。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求 capture 页面支持导出日志。该能力只生成本机内部诊断文件，不新增 WebSocket 动作或字段；为遵守人体原始数据和运维日志边界，导出内容默认脱敏，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-serial-algorithm-validation-state-gate

- 变更：USB 串口状态拆分为 not_connected、connecting、connected、validating、validation_failed、validated 和 disconnected：固定握手通过即 connected；本地算法就绪后才发送 E1；任意非空响应后才 validated 并允许实时串口数据进入录制、算法和北向转发。算法未就绪、E1 超时或控制读写异常均记录脱敏阶段日志并进入 validation_failed。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求将 USB 串口物理连接、算法准备和设备校验拆分，并以校验成功作为实时数据门禁。已同步内部预发布契约和现场文档；未收到更新已发布对外文档的授权，因此 v0.2 保持锁定。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-kylin-local-algorithm-build-and-smoke-gate

- 变更：银河麒麟 V10 x86_64 新增目标机本地算法准备流程：使用仓库锁定 SDK/NumCpp 构建 C++ bridge 到项目 .runtime/algorithm，缺少系统构建依赖时经 yes/no 授权后安装；无人体数据进程自检成功后才原子启用配置。串口启动脚本校验算法已启用、bridge 为项目内可执行普通文件，失败则禁止启动与发送 E1。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求确认 Linux 算法是否可用并直接完成下一步。本变更只实现麒麟内部构建、进程自检、启动门禁和运维文档；算法准确性仍待真实数据 POC，未收到更新已发布对外文档的授权。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-kylin-control-ack-toolchain-and-task-menu-fix

- 变更：根据麒麟实机诊断收紧 USB 串口控制门禁：E1/E0 只接受单字节 0x01，AA、周期握手和其他应答均失败且不进入数据解析；麒麟算法构建改用 SHA-256 锁定的项目内 CMake 3.31.6，并按平台校验 Eigen 3.3.7；一键助手改为可重复的一键准备并启动及完整数字快捷菜单，同时重写现场快速上手与日志排障手册。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；现场日志确认系统 CMake 3.16.3 无法满足 SDK 3.22 最低要求，现有 Eigen 锁定与麒麟 3.3.7 不一致，并且旧策略曾把握手残片 AA 当成非空控制成功；用户要求修复并简化现场操作。变更只涉及网关内部设备校验、构建与操作流程，不修改已锁定 B 端协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-kylin-network-free-startup-and-single-file-update

- 变更：修复麒麟入口每次尝试 Git 更新的问题：日常 bootstrap 只打开已有版本菜单且明确记录 sourceUpdateAttempted=false；新增开发机离线更新构建器，将已提交分支、版本和 SHA-256 封装为单个 .run，目标机只从内嵌本地 Git bundle 快进并保留项目运行数据，完成后自动进入菜单；联网 pull 仅保留为操作员主动选择的可选菜单项。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户反馈现场每次运行都会提示 git pull 不可用。根因是旧设备只收到 bootstrap 文件、缺少菜单脚本时入口会自动访问目标分支。该修复将启动与源码交付彻底分离，不改变 B 端网络协议或已锁定对外文档。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-serial-handshake-ack-validation-before-write-only-enable

- 变更：按设备串口协议修订号 181 更正控制时序：设备的单字节 0x01 是上位机回写完整 7 字节握手 ACK 后的校验结果；ACK 校验成功且算法就绪后，无响应写入 0xE1 使能采集，停止时无响应写入 0xE0。状态、日志、测试、capture 页面和内部文档同步区分 ACK 响应与无响应启停命令。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户指出 E1 只是无响应使能指令，并提供源协议文档；飞书源文档修订号 181 明确写明设备校验上位机握手 ACK 成功后返回 01，指令章节只定义 E1 使能与 E0 关闭。本次修正网关内部设备接入，不修改已锁定 B 端协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-serial-handshake-ack-window-retry

- 变更：根据麒麟实机日志修复握手 ACK 等待窗口：不再因读到周期握手首字节 AA 立即失败，而是按字节收齐完整重复握手、重新回写同值 ACK 并继续等待；仅将握手帧之外的独立 0x01 作为成功，窗口结束仍无 0x01 才失败。日志新增 ACK 写入次数/字节数、重复握手、异常字节、部分握手和总读取字节统计。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；现场提交 7fb776e 后的日志连续 25 次显示回写 ACK 约 136～158 ms 后读到 AA；旧实现只读取一个字节，无法区分完整周期握手与最终独立 01，导致过早关闭端口且永远不发送 E1。本次只修复网关内部串口状态机、日志、测试和内部文档，不修改已锁定 B 端协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-capture-frame-aligned-eeg-hr-display

- 变更：修复 capture 页面同窗数据先集中打印全部 EEG、再集中打印全部 HR 并自动滚到底部，造成现场看似只有 HR 的显示问题；页面现按包序将同一设备帧的序列号、六路 EEG 和 HR 合并为一行，包数不一致或单侧缺失时明确提示且不丢弃数据。600ms 窗口改用本地时分秒显示，页面丢包统计与网关的 16 位序列号回绕、重复包和乱序补包口径对齐。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户现场看到同一 600ms 窗口连续多行 HR [00]，实际原因是当前页面的分流打印顺序和自动滚动，而非网页未订阅 EEG；随后要求解释长整数窗口和丢包率。本次只调整本机 capture 展示与页面统计，不改变 WebSocket 动作、字段或已锁定 B 端协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-serial-powered-session-resume-without-handshake

- 变更：登记并修复网关异常退出后的串口恢复风险：设备只在重新上电后发送握手，0xE0 只停止脑电数据且不表示断开。同一设备首次握手 ACK 校验成功后持久化严格 USB 身份；网关或 systemd 重启时，匹配设备可在算法就绪后直接无响应发送 0xE1，并以首个完整合法 28 字节帧作为 validated 门禁。身份不匹配时禁止盲发控制命令。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户确认设备只有重新上电才发送握手，发送 0xE0 只是停止脑电数据而非断开，也不需要重新发送握手协议数据；随后要求修复，并特别要求先标记网关服务意外关闭后 USB 不发送握手的风险。本次只修改网关内部设备状态机、运行状态、测试和内部文档，不修改已锁定 B 端协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-02：2026-09-02-serial-resume-observe-capture-before-e1

- 变更：收紧免握手恢复的控制决策：严格身份匹配后先在配置的数据有效性窗口内被动检查完整合法 28 字节帧。设备已在发送脑电时直接接管现有数据流且不重复发送 0xE1；仅在设备静默时发送 0xE1，并继续以命令后的首个合法帧作为 validated 门禁。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户指出不能对匹配设备一律直接发送 0xE1，必须先看当前设备是否正在发送脑电数据。该修正只改变网关内部串口恢复状态机、日志、测试和内部文档，不修改已锁定 B 端协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-03：2026-09-03-serial-active-ack-and-e1-frame-gate

- 变更：按最终设备行为更正串口时序：设备不主动发送握手协议，网关对候选主动发送固定 7 字节 ACK，收到独立 0x01 后选中目标；算法就绪后必须发送无响应 0xE1，设备只有收到 0xE1 才发送 28 字节脑电帧，首个合法帧到达后才进入 validated。移除持久身份、免握手恢复和已有流接管分支；服务重启可重新执行 ACK、0x01、0xE1，因此原异常退出后无法恢复的协议风险关闭。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户最终澄清耳机不会主动发握手，但收到网关 ACK 后会返回 01；并指出只有发送 0xE1 后耳机才会发送 28 字节脑电帧。该修正只影响网关内部串口状态机、配置、测试和内部文档，不修改已锁定 B 端协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-03：2026-09-03-serial-validation-by-ack-or-existing-stream

- 变更：纠正上一条串口状态语义：新打开候选若已在发送合法 28 字节脑电帧则直接进入 validated、接管现有流并跳过 ACK 与 0xE1；静默候选在网关主动 ACK 后返回独立 0x01 时立即进入 validated。0xE1 仅在 ACK 路径且算法就绪后使能采集，不是验证指令，其后的首帧也不参与设备验证。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确纠正：ACK 后的 0x01 就表示 validated，0xE1 只是使能指令；同时设备可能已经在发送脑电数据，此时合法帧本身即可确认 validated。本次仅修正内部实现、测试、配置注释和内部文档，不修改已锁定 B 端协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-03：2026-09-03-pr13-review-contract-and-device-boundary-fixes

- 变更：修复 PR #13 审查项：串口内部状态统一映射为已发布北向三态，专网监听限制为 RFC1918 IPv4，固定串口路径限制为真实 USB 派生 TTY，无设备时不猜测授权组，休眠 BLE 配置不解析，并纠正验证与 E1 时序说明。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求修复 PR #13 review 问题；变更恢复并强化已发布 v0.2 兼容性，未授权改写已锁定对外协议 Markdown。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-04：2026-09-04-capture-static-assets-without-query

- 变更：修复网关托管 capture 页面只显示裸 HTML 的问题：移除 CSS 和主脚本地址上的版本查询参数，使资源请求符合回环 HTTP 服务的无查询参数安全约束；保留 no-store 响应头避免旧资源缓存。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；麒麟现场截图显示 capture 页的 HTML 已加载但样式和脚本未执行；根因是页面生成带 ?v= 的静态资源请求，而网关明确拒绝查询参数。本次只修复本机页面资源装载，不改变 WebSocket 契约或已锁定对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-04：2026-09-04-kylin-daily-startup-without-git-permission-repair

- 变更：日常 bootstrap 和菜单 1 不再检查或修复 Git 所有权，只在 .runtime 实际不可写时修复运行目录；sudo 串口配置禁止生成 root 所有的源码 __pycache__，完整诊断使用 Git no-optional-locks 读取状态，避免刷新并替换 .git/index。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户反馈每次执行菜单 1 都提示修复所有权和 Git 写权限；现场日志已分别记录 root 所有的 neurobridge/serial/__pycache__ 和 .git/index，后者由 sudo 诊断中的 git status 刷新造成。本次只修复麒麟内部启动与诊断权限边界，不改变 WebSocket 契约或已锁定对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-04：2026-09-04-kylin-systemd-autostart

- 变更：银河麒麟一键助手新增开机自启管理：首次前台验证后可安装、查看、停止并禁用项目受管的 neurobridge.service；服务以当前桌面用户运行项目内配置和算法，开机启动、异常退出后自动重试，并阻止前台重复启动抢占串口或本机端口。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求增加银河麒麟系统开机自启逻辑。本次只增加内部部署、运行管理、状态与日志说明，不改变 WebSocket 契约或已锁定对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-04：2026-09-04-pr13-review-runtime-and-deployment-fixes

- 变更：修复 PR #13 review 发现的下载 Host 校验、显式设备传输配置、Ubuntu 串口服务权限、服务监听端口冲突校验和本机页面自动 WebSocket 连接，并补充对应自动化测试。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求修复 PR #13 review 问题；变更仅涉及内部配置、安全、部署和本机 UI，不修改已锁定北向协议 v0.2。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-04：2026-09-04-kylin-earphone-usb-serial-scope-and-review-fixes

- 变更：将本次需求从头环多传输修正为银河麒麟 V10 耳机 USB 串口接入，重命名并收紧内部 PRD、技术方案和接线图；修复算法状态广播、已有流读取边界时间、麒麟助手配置幂等判断、串口状态说明和耳机运行术语，并回退未获授权的预发布北向协议正文改动。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确澄清本次程序只涉及银河麒麟 V10，设备为通过 USB 串口通信的耳机，与 macOS 和头环无关，并要求修复 review 问题。本次不更新任何已发布或预发布 B 端协议语义。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-04：2026-09-04-capture-replay-clarity-fixed-vertical-layout-and-default-autostart

- 变更：根据麒麟现场诊断明确 capture 页的实时耳机连接与数据来源是两个独立状态：实时串口验证失败但录播可用时，页面醒目标记历史录播而不再造成‘断开却仍有实时数据’的误解；原始与解析区固定上下排列，不随页面宽度改为左右并排。银河麒麟一键准备和直接启动默认安装或复用 systemd 开机自启服务，只有操作员显式禁用后才持久采用非自启前台运行。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供的目标机诊断显示串口候选可打开但主动 ACK 后未收到独立 0x01，随后 capture 订阅以 mode=replay 启动历史回放；同一诊断还显示 neurobridge.service 未安装。用户要求解释状态、将原始与解析区固定为上下排列并将自启设为默认。本次只修改本机页面、麒麟部署默认值和内部说明，不改变 WebSocket 字段或已锁定对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-04：2026-09-04-serial-source-disables-replay-fallback

- 变更：收紧银河麒麟耳机 USB 串口数据源：串口未验证或断开时不再自动选择录播，getLatest 与 subscribe 返回实时数据不可用错误；串口配置拒绝填写 replay_recording_id，capture 页面和内部文档同步说明实时串口专用语义。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求耳机串口数据源不能使用录播形式。本次仅修改串口数据源的内部网关行为、配置校验、capture 文案、测试和内部文档，不修改已锁定北向协议正文；历史兼容的非串口录播逻辑保留。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-04：2026-09-04-serial-ack-validation-state-preservation

- 变更：明确串口 ACK 仅在候选打开且被动观察窗口内没有合法 28 字节流后发送；ACK 后未收到独立 0x01 时保留内部 validation_failed，不再在异常收尾阶段覆盖为 not_connected。Capture 区分浏览器到网关的 WebSocket 连接与耳机校验状态，并在实时数据请求错误中显示校验失败。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户指出唯一串口候选可以打开但 ACK 后未收到 0x01 时应显示校验失败，而不是断开，并询问 ACK 的发送时机。本次修复内部状态覆盖和本机页面表达，继续通过已锁定三态映射及既有错误包络兼容 v0.2，不修改对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-07：2026-09-07-kylin-systemd-working-directory-v245-compat

- 变更：修复银河麒麟 systemd 245 拒绝带外层引号 WorkingDirectory 导致 neurobridge.service 未安装的问题：工作目录改为原始绝对路径并仅转义 systemd 说明符，单元渲染拆为可独立执行的回归测试。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供的 2026-09-07 目标机诊断包连续两次记录 WorkingDirectory path is not absolute，并明确要求修复。本次只修复银河麒麟内部部署兼容性与测试，不改变 WebSocket 契约或已锁定对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-07：2026-09-07-capture-refresh-status-after-subscribe

- 变更：修复 capture 页面在耳机于首次状态查询后、开始订阅前完成连接时仍显示旧连接状态的问题：订阅成功后立即重新查询网关状态，随后继续由 status 事件维护状态。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供的目标机诊断显示页面首次 getStatus 后耳机才完成独立 0x01 校验，而页面在订阅前不会收到该状态事件；点击开始接收后数据正常但状态快照未刷新。本次只修复本机 capture 页面状态同步，不改变 WebSocket 动作、字段或已锁定对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-07：2026-09-07-capture-realtime-device-status-subscription

- 变更：将 capture 的设备状态订阅与 EEG/心率数据订阅拆分：网页连接网关后立即订阅 status 并读取当前快照，设备离线时也允许纯状态订阅，使连接、校验和断开变化无需点击开始接收即可实时推送；开始和停止按钮只控制数据流。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确指出设备状态应实时分发和获取，不能依赖点击开始接收。本次复用既有 subscribe、getStatus 和 status 事件，修复服务端纯状态订阅的离线门禁及本机页面订阅生命周期，不新增或修改协议字段，未获授权改写已锁定对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-07：2026-09-07-capture-complete-serial-frame-lines

- 变更：将 capture 原始数据区从每个设备帧分别打印 20 字节 EEG 和 1 字节 HR 两行，改为按已确认串口结构还原并显示单行完整 28 字节帧；包数不一致时拒绝生成伪完整帧，解析区继续报告异常。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求 capture 显示完整帧结构。本次仅调整本机页面对既有 eeg.raw 与 hr.raw 的展示和导出，不新增或修改 WebSocket 字段，不改写已锁定对外协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-07：2026-09-07-multi-system-architecture-m1-implementation

- 变更：按多系统项目结构 PRD 与技术方案落地稳定领域内核、Ports、M1 Deployment Profile 和唯一 Bootstrap；银河麒麟串口链路接入纯 28 字节 Parser，并增加双状态、原子快照、慢消费者覆盖、算法超时/迟到隔离及有界分段持久化。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求实现内部 PRD 与技术方案，但未明确授权更新已发布或预发布 B 端协议；本轮保持 v0.2 字段和错误映射不变，存储状态新增北向合同继续受发布门禁约束。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-07：2026-09-07-serial-replay-policy-rule-consistency

- 变更：修正仓库级规则与耳机专用 capture 页中的旧录播回退表述：只有 supports_replay=true 的蓝牙头环兼容 Profile 可使用录播；银河麒麟及后续 Windows 耳机串口 Profile 固定禁用 replay，离线或存在历史录制时均不得启动录播。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户再次明确耳机串口模式不能使用录播，并要求修复仓库规则冲突；本次同步 AGENTS、内部 PRD/技术方案、耳机专用页面文案和回归测试，不修改已锁定的头环北向协议 v0.2 或内部预发布协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-07：2026-09-07-multi-system-architecture-production-completion

- 变更：补齐统一生产 ApplicationService、会话绑定 DeviceControl E1/E0、独立 NorthboundController、存储恢复防抖与配置迁移；macOS/Ubuntu BLE 入口统一接入 Bootstrap，并增加 Windows COM/Service 源码、银河麒麟与 Windows 无签名候选包及分层自动化测试。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求完成内部多系统 PRD 中当前开发机可实现的剩余功能，但未授权修改已锁定 v0.2 或内部预发布协议正文；目标机真实设备验证、签名发布和各平台 24 小时长稳仍需外部环境与发布审批。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-08：2026-09-08-production-pipeline-resilience-and-requirement-followup

- 变更：补齐生产 Application 与 DeviceControl/Fanout/ReplayReader 接线，隔离慢算法和磁盘等待，修复断线清理、串口来源/序列追踪、20 字节报文投影与新会话旧快照；算法失步桥接退出防止跨请求误配；分段单写并兼容历史读取/下载，增加周期资源观测、麒麟候选回滚和可追溯构建元数据。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求根据两组内部 PRD/技术方案补齐未完成需求并优化。当前请求未明确授权更新对外协议，因此保持发布与预发布正文及版本不变；存储新字段、目标运行时/签名/原生包和真实设备 24 小时验收继续按门禁推进，不宣称已交付。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-08：2026-09-08-requirement-audit-code-fixes

- 变更：修复需求核对发现的配置类型强转、Kylin 版本漏检、600ms 窗口不一致、Capture 重连顺序和麒麟日志无轮转；补齐 BLE 会话控制、北向投影注入、批次元数据、可选有界 RawChunk 留存、存储 errno 分类/启动降级/恢复与清理审计、按流观测以及候选包完整性和磁盘预检。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求优先修复可通过代码解决的需求；本轮恢复已确认行为并完善内部实现，不授权修改已发布或预发布北向正文。存储新增北向合同、最终镜像/算法真实数据、正式运行时/许可证/签名及各平台现场验收仍需后续关闭。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-pr13-ack-algorithm-status-console-log-fixes

- 变更：修复 PR #13 审查项：ACK 出现未知响应后拒绝本轮嵌入的 0x01；运行中的算法失败同步内部状态和既有 status 订阅；麒麟启动脚本取消无轮转的控制台日志副本，保留 Python 轮转日志与终端/journal 输出。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求修复上述三项 review 问题；本轮修复内部验证、状态同步和日志可靠性，不修改已发布或预发布北向协议正文，目标银河麒麟真实设备回归仍需执行。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-internal-operations-tutorial

- 变更：在麒麟内部运行手册补充 Windows PowerShell 源码联调教程，覆盖 Python 环境、USB COM 驱动与枚举、独立配置、算法 bridge 前提、前台启停、本机页面、日志排障及已安装候选服务操作，并增加 README 导航。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户指出内部手册缺少 Windows 操作教程；本次仅补充与现有源码对应的内部操作说明，不改变实现、部署默认值或正式验收范围，未授权更新对外协议。Windows 实机、Windows 7 运行时、签名及长稳仍待验证。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-one-click-source-launcher

- 变更：新增 Windows 双击 CMD/PowerShell 一键菜单，准备项目 Python 3.11 x64 与依赖、首次配置、COM 检查、算法空输入自检、端口和重复进程预检、前台启停及诊断摘要；支持不联网的离线模式，增加配置保留与失败门禁回归及 Windows CI。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求 Windows 提供类似麒麟的一键脚本；本次提供 Windows 10/11 源码联调入口，复用现有串口与正常 Bootstrap，不修改已发布或预发布北向协议及麒麟入口。Windows bridge 仍须预先提供，真实设备、Windows 7 运行时、服务安装发布和长稳仍待验证。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-double-click-default-start

- 变更：Windows 入口无参数时直接执行准备并启动，不再要求输入菜单数字；排障菜单改为显式 -Action menu，保留错误窗口、已有配置与离线参数，并同步操作文档及 Windows 离线流程测试。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求简化为双击运行；仅调整 Windows 启动器默认动作，不改变北向协议、算法前提或正式验收范围，未授权修改已发布和预发布协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-automatic-native-algorithm-build

- 变更：Windows 双击流程自动准备校验锁定的 LLVM-MinGW/CMake/Ninja/Eigen，复用仓库 SDK/NumCpp 构建静态运行库的 x64 算法 EXE；检查 PE/DLL 导入与空输入自检，记录源码指纹和工具链 manifest，通过后才替换并备份旧程序，支持离线缓存及源码变化重建。Windows CI 复用构建器并上传 unsigned 产物和摘要。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求补齐双击启动仍需人工提供 Windows 算法文件的缺口；本次调整 Windows 源码构建与内部操作说明，不改变设备数据分组、算法语义、北向协议或麒麟构建依赖。Windows 真实设备/算法结果、Windows 7 运行时及正式签名验收仍待完成，未授权更新对外协议。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-smoke-environment-case-fix

- 变更：修复 Windows 算法构建自检将 os.environ 转为普通字典后丢失大小写无关语义，导致读取 SystemRoot 抛出 KeyError 的问题；统一环境变量键大小写，保持隔离 PATH，缺少 SYSTEMROOT 时提供明确错误并补充回归测试。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供 Actions 34321574077 的失败链接；日志确认编译已完成，失败发生于空输入自检前的环境准备。本次仅修复内部 Windows 自检兼容性，不改变算法、设备协议或已发布及预发布北向协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-user-confirmed-windows-kylin-runtime-validation

- 变更：记录用户确认 Windows 10 专业版 22H2（OS 内部版本 19045.6466，x64；Inspiron 7559、i7-6700HQ、16 GB RAM）及银河麒麟 V10 SP1 2503 运行联调成功，同步内部手册、验收清单与 README；系统与硬件由用户提供的两张截图确认。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确确认两种系统验证成功，并先后提供 Windows 设备规格和系统版本截图；仅记录本次现场反馈，不推断未提供的版本、部署提交或专项测试结果，不改变正式交付范围及已发布和预发布协议正文，不保存设备标识或产品 ID。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-ci-recording-finalization-race

- 变更：修复录播集成测试使用 100 ms 关闭时限导致 CI 录制收尾超时、读取未完成文件和临时目录清理竞态：正常测试恢复配置中的持久化与关闭时限，短关闭时限仅用于故障测试；独立清理确保写入线程退出，增加超过旧时限的慢收尾回归检查。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求修复 Actions 34323970712；日志确认同一录播测试先发生 session_close_timeout，再断言失败并在清理时与写入线程冲突。本次仅调整测试时限、收尾验证和资源清理，不修改生产关闭超时策略、设备或算法行为、已发布及预发布协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-project-service-autostart

- 变更：Windows 双击入口默认经管理员授权注册项目内自动服务，开机无需登录即可运行既有 Bootstrap；复用运行中服务并打开本机页面，支持查看状态、关闭自启正常停止及保存前台偏好，增加独立宿主/管理轮转日志、异常重启、同名安装保护和 Windows SCM 生命周期 CI 测试。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确要求 Windows 支持开机启动；本次仅调整 Windows 内部启动部署和操作文档，不改变设备、算法、已发布及预发布北向协议。服务使用固定本地项目路径及 LocalSystem，新增开机自启、停机及故障恢复仍需 Windows 目标机验证，不能沿用此前前台联调结论。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-recording-fsync-and-rotation-recovery

- 变更：修复 Windows 录制索引对只读句柄 fsync 导致每 10 分钟切段失败：通过可写句柄完成写入、flush、fsync 后原子替换；切段失败移除关闭句柄，索引提交失败退回可恢复 partial，清理临时索引；关闭时逐类收尾并记录失败，未完成分段不标记为完整会话，临时恢复 I/O 故障不隔离为坏数据。新增连续切段、故障隔离与重启恢复回归并接入 Windows CI。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户提供 Windows neurobridge.log 并明确要求修复；日志确认串口持续收帧但切段保存失败。本次修复内部持久化兼容性，不改变设备、算法输入或已发布及预发布北向协议；旧录制缺口不能自动补回，Windows 目标机跨分段现场复测仍待执行。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-service-ci-fixture-and-process-cleanup

- 变更：修复 Windows 服务集成测试临时项目漏拷 pyproject.toml 导致启动器完整性检查失败；统一临时项目的规范路径，避免 8.3 短路径与宿主长路径产生归属检查差异；停止测试服务后等待实际进程退出并释放句柄，再重启或删除临时日志目录。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户反馈 Actions 34327671060 失败；该次 Windows 录制切段回归与算法构建已通过，失败位于服务测试的临时目录准备及清理。本次仅修复测试设施，不改变生产服务或已发布及预发布协议正文；修复后的原生服务测试仍需 CI 验证。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-windows-headset-e0-e1-restart-recovery

- 变更：按用户确认的设备恢复规则增加 Windows ACK/E1 双阶段探测：正常 E0 停止并匹配 USB/COM 身份摘要时优先 E1，否则 ACK 优先；E1 前准备隔离算法，无合法帧回退 ACK，ACK 超时可试 E1。只以合法帧或独立 ACK 01 验证，原始帧按正常会话持久化，失败/取消探测尽力 E0，复用既有超时与重连配置。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户日志确认 E0 停止后 COM4 可打开但持续无 ACK 回复，并明确授权 ACK/E1 交替及正常停止后优先 E1。仅调整 Windows 内部恢复策略，同步仓库规则、操作文档与回归；不修改已发布及预发布北向正文，麒麟策略保持不变，Windows 真机不拔插重启复测仍待执行。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-long-running-test-observability

- 变更：增强 Windows/麒麟共用运行日志：增加单调运行时长、采样序号、周期计数增量与实测帧率、事件循环延迟、完整帧年龄与窗口范围、算法结果/超时/最大耗时、最近存储成功时间、即时连接事件及停止采集汇总；沿用配置化日志轮转并补充长测操作说明。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求增加日志以开展长时间运行测试；仅增强内部统计和操作说明，不记录原始人体数据或算法数值，不改变北向字段、设备控制或已发布及预发布协议。自动化回归不替代 Windows 与银河麒麟真实设备长稳及故障恢复验收。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-09：2026-09-09-timestamped-size-log-rotation

- 变更：运行日志大小轮转改用 UTC 微秒时间与独占随机后缀归档，避免同时间或重启覆盖、取消历史归档编号移动；按实际编码字节计量，保留完整超长单条记录，按配置清理时间归档与旧编号备份，临时删除失败不丢弃新日志并在下次轮转重试。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求限制单个日志大小并让文件名关联时间以避免重复；保持当前日志固定入口、已有查看和导出兼容，补充内部手册及 Windows 原生轮转 CI，不修改已发布或预发布北向协议，目标机实际轮转仍需随长测确认。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-10：2026-09-10-device-confirmed-ack-after-handshake

- 变更：记录设备端确认握手成功后重复 ACK 不再响应 0x01；同步耳机串口与多系统 PRD、技术方案、运行手册、验收清单及仓库摘要/规则，区分首次握手与已握手恢复，撤回仅重试 ACK 即可关闭重启恢复风险的表述，明确 Windows ACK/E1 验证依据与麒麟已握手无流恢复缺口。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户向设备端确认后明确要求更新对应 PRD 和技术文档；本次仅补充内部设备行为事实，不修改运行代码、平台策略、版本号或已发布及预发布北向协议。具体握手复位条件仍待设备端确认，设备行为确认与模拟回归不替代目标机停止/整机重启恢复专项验收。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-10：2026-09-10-serial-direct-e1-parallel-startup

- 变更：按设备端确认的首次上电无握手也可 E1 出数规则，统一 Windows/麒麟并行串口发现与算法准备，移除 ACK/01 和停止提示恢复；两项就绪后 E1，以完整合法帧验证，复用预备算法并保留首帧读取时间，失败/取消 E0 清理，同步内部需求、方案、操作文档和界面提示。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户明确提出移除 ACK、并行发现与算法准备，并确认设备未握手直接 E1 可出数；本次替代旧 ACK/E1 策略，旧配置键兼容保留且不再等待应答。复用既有北向状态与错误、不修改字段或已发布及预发布协议正文；源码/模拟回归不替代 Windows 和银河麒麟的真实冷启动、停止/整机重启、拔插及离线拒绝录播验收。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-10：2026-09-10-windows-log-rotation-test-encoding

- 变更：日志轮转测试显式使用 UTF-8 读写文本，与生产日志编码保持一致，修复 Windows 默认 CP1252 解码中文日志时的 UnicodeDecodeError。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户反馈 Actions 34430971789 失败；错误已在本地模拟 CP1252 默认读取时复现。本次仅修复测试的编码假设，不改变生产日志轮转行为、设备接入或已发布及预发布协议正文。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-09-10：2026-09-10-windows-service-diagnosis-double-click

- 变更：新增可双击 CMD/PowerShell 服务诊断入口：请求管理员权限，记录启动前后服务与 Python 进程快照，校验项目目录/账户后仅启动已停止服务，保存唯一时间命名报告与服务宿主日志尾部；不结束进程、删除锁或修改自启，并补充 Windows 模拟服务流程 CI。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求把锁占用排查和 Start-Service 命令做成双击脚本；仅增加内部运维工具，现场锁持有者仍须依据诊断确认，不修改设备、算法或已发布及预发布北向协议。Windows UAC、实际 SCM 与锁冲突恢复仍需目标机验证。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。
