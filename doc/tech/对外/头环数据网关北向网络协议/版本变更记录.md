# 版本变更记录

本文件由 `neurobridge/version_registry.toml` 生成；版本台账是唯一来源。已锁定区间不可回写，未锁定变更不会修改已发布对外文档。

## 已锁定的发布区间

### v0.1 → v0.2

- 状态：`locked`；锁定时间：2026-08-05。
- 锁定依据：已发布的 B 端 头环数据网关北向网络协议 v0.2 PDF。
- 约束：此区间的 Markdown/PDF 及其语义不再回写；后续变更只能创建新的未锁定记录。

## 当前内部预发布版本

- 版本：v0.3；状态：`prerelease`。
- 基线：v0.2；仅保留此一份内部预发布 Markdown。
- 升格规则：收到明确发布指令后，将该文档转换为独立的对外版本 Markdown，更新台账和锁定记录；CI 成功生成该版本 PDF Artifact 后方可合入发布。

## 未锁定变更

### 2026-08-05：2026-08-05-replay-get-latest-cursor

- 变更：录播中 getLatest 优先返回当前 WebSocket 连接已回放的有效算法结果；尚未订阅时返回录制中的最新有效算法结果。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；未收到更新对外文档的明确指令；运行时保持已发布 v0.2 的错误码兼容性。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-05：2026-08-05-flowtime-document-profile-poc

- 变更：macOS 采集 POC 按已核对的 Flowtime 文档订阅 FF31、FF32、FF51 并使用 FF21 启停；移除不存在的 FF52 订阅。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；已发布 v0.2 保持锁定；未收到更新对外文档的明确指令，且对外协议不应暴露设备 BLE 特征或固定包长。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。
