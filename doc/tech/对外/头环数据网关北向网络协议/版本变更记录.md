# 版本变更记录

本文件由 `neurobridge/version_registry.toml` 生成；版本台账是唯一来源。已锁定区间不可回写，未锁定变更不会修改已发布对外文档。

## 已锁定的发布区间

### v0.1 → v0.2

- 状态：`locked`；锁定时间：2026-08-05。
- 锁定依据：已发布的 B 端 头环数据网关北向网络协议 v0.2 PDF。
- 约束：此区间的 Markdown/PDF 及其语义不再回写；后续变更只能创建新的未锁定记录。

## 当前内部预发布版本

- 版本：v0.3；状态：`prerelease`。
- 基线：v0.2；仅保留此一份内部预发布 Markdown。
- 升格规则：收到明确发布指令后，将该文档转换为独立的对外版本 Markdown，更新台账和锁定记录；CI 成功生成该版本 PDF Artifact 后方可合入发布。

## 未锁定变更

### 2026-08-05：2026-08-05-capture-package-pdf-export

- 变更：一键保存 ZIP 在会话根目录附带已发布的头环数据采集包格式说明 v0.1 PDF，并在 manifest 的 documentation 字段记录其路径、版本和 SHA-256。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求调整导出物；已发布的采集包格式说明 v0.1 保持锁定，未改写其 Markdown。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-05：2026-08-05-replay-get-latest-cursor

- 变更：录播中 getLatest 优先返回当前 WebSocket 连接已回放的有效算法结果；尚未订阅时返回录制中的最新有效算法结果。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；未收到更新对外文档的明确指令；运行时保持已发布 v0.2 的错误码兼容性。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-05：2026-08-05-flowtime-document-profile-poc

- 变更：macOS 采集 POC 按已核对的 Flowtime 文档订阅 FF31、FF32、FF51 并使用 FF21 启停；移除不存在的 FF52 订阅。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；已发布 v0.2 保持锁定；未收到更新对外文档的明确指令，且对外协议不应暴露设备 BLE 特征或固定包长。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-05：2026-08-05-macos-native-affective-bridge-poc

- 变更：macOS POC 接入锁定的 AffectiveCloud C++ SDK，构建本地算法 bridge 并输出既有算法字段结构；已完成源码构建和合成输入烟雾测试，未完成真实数据或 Ubuntu 验证。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；对外 v0.2 保持锁定，且未收到更新对外文档的明确指令；本变更不增加或修改 B 端可观察字段。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-06：2026-08-06-confirmed-device-profile-restoration

- 变更：恢复已确认的 FF31=14、FF51=16、FF52=20 字节 profile；订阅并持久化三类原始通知，FF52 用于北向 hr.raw。录播按各流的实际包长生成元数据。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；修复实现与已锁定 v0.2 原始数据字段契约不一致的问题；未改写已发布对外 Markdown。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。

### 2026-08-06：2026-08-06-ubuntu-download-and-persistent-operations

- 变更：Ubuntu x86_64 网关新增 systemd 即装即启、文件化持久运行日志，以及专用有线网络上的已结束录播 ZIP 与日志快照下载 HTTP 服务。
- 锚点：已锁定的 v0.2；目标对外版本：`0.3`。
- 对外文档：`record_only`；用户要求增加部署与运维能力，但未明确授权更新已锁定的 B 端对外协议；下载地址、端口和启用状态均由部署配置控制。
- 发布条件：收到明确“更新对外文档”授权后，创建下一版本的 Markdown/PDF、发布锁定记录和文件摘要；不得修改已锁定区间。
