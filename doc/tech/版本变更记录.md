# 版本变更记录

所有当前版本号、文档角色和发布状态以 [版本台账](../../neurobridge/version_registry.toml) 为唯一来源；本文件只记录变更决策，不重复维护版本号。

## 2026-08-05：录播 `getLatest` 游标语义

- 变更：录播中 `getLatest` 返回当前 WebSocket 连接已回放到的算法结果，不预读录制末尾。
- 对外文档：未更新；原因是未收到“更新对外文档”的明确指令。
- 后续：如需对 B 端发布该行为，应由用户明确授权，再更新版本台账中的对外版本/发布日期、对应 v0.2 Markdown 与 PDF，并完成协议兼容性和 PDF 检查。
