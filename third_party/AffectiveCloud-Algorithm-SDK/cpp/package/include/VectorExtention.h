#include <vector>
#include <cstdint>
#include "TypeDefine.h"
namespace basic
{
    namespace tools
    {
        
        std::vector<uint8_t> cutArrs(std::vector<uint8_t>& arrs, int begin, int end);
        vectord cutArrs(vectord &arrs, int begin, int end);
    } // namespace tools
    
} // namespace basic
