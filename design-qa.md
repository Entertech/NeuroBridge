# Capture 页面设计 QA

## 比较目标与证据

- 宽屏设计基准：`/Users/chengpeng/.codex/generated_images/01a061a7-2fdc-7432-aeb9-99acdbc9e1a3/exec-79106da8-fc89-4ffe-9430-8457da3d43ce.png`
- 窄屏设计基准：`/Users/chengpeng/.codex/generated_images/01a061a7-2fdc-7432-aeb9-99acdbc9e1a3/exec-cfb4a2f7-3fcd-4b1f-988e-1816a76a0941.png`
- 宽屏实现截图：`/Users/chengpeng/.codex/visualizations/2026/09/02/01a061a7-2fdc-7432-aeb9-99acdbc9e1a3/implementation-wide-data-region.png`
- 窄屏实现截图：`/Users/chengpeng/.codex/visualizations/2026/09/02/01a061a7-2fdc-7432-aeb9-99acdbc9e1a3/implementation-narrow.png`
- 同屏宽屏比较：`/Users/chengpeng/.codex/visualizations/2026/09/02/01a061a7-2fdc-7432-aeb9-99acdbc9e1a3/design-comparison-wide.png`
- 同屏窄屏比较：`/Users/chengpeng/.codex/visualizations/2026/09/02/01a061a7-2fdc-7432-aeb9-99acdbc9e1a3/design-comparison-narrow.png`
- 状态：深色主题；使用合成的 2 个 EEG 包和 2 个 HR 包；原始数据以 16 进制显示，另验证了 10 进制状态。
- 宽屏视口：1280 × 720 CSS px，设备像素比 1；设计基准为 1487 × 1058 px，实现数据区为 1225 × 490 px。比较时在同一 1280 × 720 页面按各自宽度等比缩放，没有把像素密度差异视为设计问题。
- 窄屏视口：使用 760 CSS px 宽的同源内嵌视口验证真实媒体查询，实现全页截图为 1265 × 3200 px；设计基准为 1487 × 1058 px。同屏证据用于核对内容顺序和布局转换，不作逐像素高度比较。

## 全视图比较

- 宽屏实现与方案 2 一致：原始数据在左、解析结果在右，两个面板等高，进制切换和原始导出位于原始数据标题区。
- 窄屏实现与方案 1 一致：原始数据在上、解析结果在下，控件不遮挡标题和数据内容；页面其余既有连接与状态区保持原顺序。
- 设计图中的完整 28 字节示例没有照搬。当前北向契约只提供 `eeg.raw` 的 20 字节包和 `hr.raw` 的 1 字节包，实现按实际收到的包逐行保留，避免伪造设备帧头尾。

## 聚焦区域比较

- 对原始数据面板作了单独截图比较，确认每行只有 `YYYY-MM-DD HH:mm:ss.SSS` 和空格分隔字节，没有 `SEQ`、`EEG`、`HR`、窗口、包号或提示文本。
- 对解析面板作了同屏比较，确认解析记录使用原始包的窗口结束时间戳，且字段值随 16/10 进制按钮同步转换。
- 对窄屏全页作了聚焦检查，确认 760 px 下 `.data-workspace` 为单列，原始区和解析区均无横向溢出或控件重叠。

## 必查表面

- 字体与排版：沿用现有 Inter/系统中文字体与等宽数据字体；标题、辅助文案和数据层级清楚，窄屏换行可读。
- 间距与布局节奏：数据面板沿用现有 18 px 圆角、边框和半透明表面；宽屏两列间距 28 px，窄屏恢复单列，未发现 P0/P1/P2 问题。
- 颜色与视觉令牌：复用页面现有背景、强调色、边框、正文和解析结果颜色，激活/禁用状态对比明确。
- 图像与资产：目标和实现均无业务图片、Logo 图片或插图；没有用 CSS/SVG/占位图替代资产。
- 文案与内容：原始区明确“未解析、未改写”；解析区明确时间戳和进制关系；诊断导出与原始导出的隐私边界不混淆。
- 图标：实现没有新增图标；导出按钮使用与页面其他文字按钮一致的既有样式，因此不存在图标缺失。
- 状态与交互：已验证空状态、收到数据后的启用状态、16/10 进制切换、原始导出成功反馈、宽/窄布局切换；最终页面加载未产生新的产品脚本错误。
- 可访问性：进制切换保留 `aria-pressed`，数据区保留 `aria-live`，按钮禁用态明确，44 px 基础点击高度在窄屏仍可用。

## Findings

- 无 P0、P1 或 P2 问题。
- P3：设计图用表格和单条选中态展示更多模拟记录，实现沿用现有实时日志形态。真实连续数据到达后密度自然增加；增加行选择属于后续交互范围，不影响本次“原始保真、解析转换和响应式布局”的目标。

## 比较历史

1. 首轮宽屏实现只有数据内容区域带边框，和方案 2 的完整面板分组相比层级偏弱，记录为 P2。
2. 已为 `.data-panel` 增加边框、18 px 圆角、内边距和半透明背景，并重新捕获宽屏、窄屏及同屏比较证据。
3. 复查后两个断点的结构、层级、控件位置与选定方案一致，没有剩余 P0/P1/P2。

## 实现检查清单

- [x] 宽屏自动使用左右两列。
- [x] 宽度不足时自动改为上下布局。
- [x] 原始区仅显示毫秒时间戳和实际收到的字节。
- [x] 原始字节及解析字段支持 16/10 进制切换。
- [x] 原始数据可独立导出，且不混入解析或诊断内容。
- [x] 诊断日志继续脱敏原始负载。
- [x] 浏览器主交互、响应式状态和控制台已检查。

final result: passed
